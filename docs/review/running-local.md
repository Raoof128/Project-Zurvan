# Running Locally

To start the review workbench:

```bash
zurvan review serve
```

By default it binds to `127.0.0.1:8765`. It will block binding to `0.0.0.0` unless `--allow-lan` is passed.

To open your browser automatically:
```bash
zurvan review serve --open
```
